---
import MenuWrapper from "@/components/Wrappers/menus";
import Layout from "../../layouts/Layout.astro";
const { searchParams } = Astro.url;
const page = searchParams.get("page") || 1;
let USD_RATE = 26000;
---

<Layout>
	<MenuWrapper client:load rate={USD_RATE} page={page} />
</Layout>
